# Direct runtime requirements plus the currently validated OCR stack.
# These components are pinned together so rebuilding the image cannot silently
# change PaddleOCR/PaddleX/OpenCV/NumPy behavior for an existing PLAI release.
# Review coupled OCR-stack upgrades together rather than bumping them blindly.
paddleocr==3.7.0
paddlex==3.7.2
requests==2.34.2
numpy==1.26.2
opencv-contrib-python==4.10.0.84
Pillow==12.3.0
shapely==2.1.2
pyclipper==1.4.0
